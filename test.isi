int x = 5;

string String(int in){
    #string concat automatically turns the type into string#
    return in + "";
}

int main(){
    if (x > 4){
        return 0;
    }
    else
    {
        x = x + 1;
        throw("x isn't bigger than 4 (" + String(x-1) + ")",1);
    }
}

exit(main());